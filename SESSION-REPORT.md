# 세션 보고서 — 2026-08-16 ~ 08-17

전부 `main`에 커밋·푸시 완료. 커밋 9개, 28개 파일, +3,142 / −643 줄.

시작은 "무겁고 느린 게 젤 큰 문제"였고, 거기서 성능 → 데이터 안전성 → RightFont 정리 →
중복 제거 재설계 → Fork 점검까지 이어졌습니다.

---

## 한눈에 보기

| 항목 | 이전 | 이후 |
|---|---|---|
| 사이드바 렌더 1회 | 6,775 ms | **0.1 ms** |
| 즐겨찾기 토글 → 재렌더 | 169.9 ms | **6.6 ms** |
| 캐시 디코드(실행 시) | 1,313 ms · 메인스레드 차단 | 1,227 ms · **백그라운드** |
| 정착 메모리 | 437 MB | 428 MB (라이브러리 37% 증가에도) |
| 중복 삭제 제안 | 71,701개 (**34%가 다른 폰트**) | 44,159개 (**0%**) |
| Fork 글리프 추출 | 517 / 657 | **657 / 657** |
| Fork 합성 글리프 | 전부 평탄화 | **528개 구조 보존** (TrueType) |
| Fork 커닝 | 전부 소실 | **47,017 페어** (`kerning.plist`) |
| RightFont 컬렉션 임포트 | 225개 중 90개 | **155개** |

라이브러리 규모: 77,837 → **106,950 faces** (RightFont 패키지에서 10,006개 추출)

---

## 1. 성능 — 진짜 원인은 폰트 개수가 아니었습니다

`SidebarView`가 body를 그릴 때마다 `itemsInSource()`를 **9번** 부르고 있었고,
그 함수가 전체 항목을 `standardizedFileURL`로 순회했습니다(1회 419 ms).
**1회 렌더에 6,775 ms.**

여기에 검색어(`searchInput`)가 `FontLibrary`의 `@Published`였고 14개 뷰가 이 객체를
통째로 구독 중이라, **키 한 번 = 전체 UI 재렌더 = 약 7초**였습니다.
Session 2에서 넣었던 180 ms 디바운스는 이 구조 때문에 **사실상 죽은 코드**였습니다.

고친 방법:
- 소스/파운드리를 **인덱스**로 버킷팅해 캐시 (FontItem 복사본을 담으면 맵당 17 MB 낭비)
- URL 정규화를 스캔 시점 1회로
- 선택·프리뷰·검색 상태를 `FontLibrary`에서 분리 (`Services/UIState.swift`)
- 무효화를 2단계로 분리 — 별표 하나 눌렀다고 중복 그룹·파일크기 정렬을 다시 계산하지 않도록
- 캐시 읽기/쓰기를 메인스레드 밖으로

**남은 것:** 실행 시 피크 메모리가 549 → 712 MB로 늘었습니다. 디코드가 메인스레드를
막는 대신 UI 구성과 동시에 돌기 때문입니다. 근본 해결은 60 MB JSON 캐시를 버리는 것.

---

## 2. ⛔️ 제가 낸 사고 — `state.json` 삭제

가장 중요한 항목이라 숨기지 않고 적습니다.

제가 만든 성능 측정 도구(`--bench`)가 `FontLibrary`를 새로 만들면서 **기존 설정을
불러오는 단계를 건너뛰었습니다.** 그 빈 상태에서 즐겨찾기 토글 속도를 재느라 별표를
눌렀고, 그게 자동 저장을 호출해 **빈 내용이 진짜 `state.json`을 덮어썼습니다.**
측정을 6번 돌렸으니 6번 덮어썼습니다.

- **잃은 것:** 스캔 폴더 전부(폰트 69,700개의 출처), 즐겨찾기 전부, 당시 프로젝트
- **되찾은 것:** 스캔 폴더 4개 — 캐시에 남은 폰트 파일 경로를 역추적해 복원
- **복구 불가:** 즐겨찾기

폰트 목록 파일은 멀쩡해서 화면상 폰트가 다 보였고, 그래서 몇 시간 동안 몰랐습니다.

**재발 방지 (2겹):**
1. `Persistence.readOnly` — 쓰기 잠금. 측정 도구는 첫 줄에서 이걸 켭니다. `--bench` 실행 후
   `state.json`이 1바이트도 안 바뀌는 것을 md5로 확인했습니다.
2. 저장 직전 이전 버전을 `StateBackups/`에 복사, 최근 30개 보관

`state.json`은 이 앱에서 **유일하게 복구 불가능한 파일**입니다(폰트 목록은 재스캔하면 됨).
HANDOFF.md 불변조건에 명시해뒀습니다.

---

## 3. 중복 제거 — 쓰면 안 되는 상태였습니다

기존 도구는 **PostScript 이름**이 같으면 중복으로 보고 "여분" 삭제를 제안했습니다.
실제 그룹 400개를 열어 내용을 비교한 결과:

- **34%가 이름만 같고 내용이 다른 별개 폰트** — 다른 버전, 다른 굵기 세트, 다른 언어판
- 서체 단위로 세면서 **파일 단위로 삭제** — 한 파일에 252개 서체가 든 것도 있어,
  "여분" 하나 지우면 무관한 251개가 함께 사라짐
- 133개 그룹은 "지킬 것"과 "여분"이 **같은 파일 안에** 있었음

**바꾼 것:** 판단 기준을 이름 → **내용(바이트 단위 해시)** 으로. 크기 버킷 → 앞뒤 16 KB 해시
→ 전체 해시의 3단계라 81,507개 파일을 **10초**에 훑습니다.

내용이 같으면 지워도 잃는 게 없습니다(사본에 모든 서체가 그대로 있음). 감사 결과:

```
동일 그룹 23,644 · 삭제 가능 44,159 · 회수 5.92 GB
남길 파일 없는 그룹 : 0   (0이어야 함)
쌍둥이 없는 서체    : 0   (0이어야 함)
판정: 안전
```

부수적으로 발견해 고친 것: `itemsAtPath`가 호출마다 전체를 훑어 **약 73억 번** 연산이
되고 있었습니다(멈춘 것처럼 보였음).

**주의:** 삭제 대상 44,159개 중 **32,387개가 Dropbox 안**에 있습니다. UI에 `Cloud`
배지로 표시됩니다.

---

## 4. RightFont 정리

`Jisung/Base NY | Fonts/` 안의 패키지에서 폰트를 꺼내 패밀리별로 정리했습니다.

- 폰트 **10,006개** → 패밀리 폴더 **1,350개**
- 내용 동일 중복 493개 제거, 이름 충돌 15개는 번호 붙여 보존
- RightFont가 엉뚱한 곳(`metadata/`)에 넣어둔 폰트 4개 발견·회수

**임포트 기능 자체가 버그였습니다.** RightFont는 고유번호를 하이픈 있는/없는 두 형태로
섞어 쓰는데, 앱이 저장할 때는 하이픈을 떼고 찾을 때는 안 뗐습니다. 그래서 우연히 맞는
것만 걸렸습니다 — **225개 중 90개**. 고친 뒤 **155개**.

**놓친 것:** 이름이 겹치는 컬렉션끼리 덮어써서 155 → 88개로 줄었습니다. 코드는 합치도록
고쳤지만, 재실행하려니 패키지를 넣어둔 휴지통이 비워져 있었습니다. Base NY 팔레트는
이후 전량 삭제하기로 하셔서 실질 영향은 없습니다.

---

## 5. 기본 기능 보강

- **다중 선택** — ⌘클릭(개별), ⇧클릭(범위), ⌘A, Esc. 2개 이상 고르면 상단에 막대가
  뜨고 일괄 활성/비활성/프로젝트 추가. 우클릭도 선택 전체에 적용
- **소스 연결 상태** — 10초마다 확인해 끊기면 흐리게 + `Offline` 배지, "파인더에서 보기"
  비활성화. **끊겼다고 폰트 목록을 지우지 않습니다**(외장하드 뽑았다고 라이브러리가
  날아가면 안 되니까)
- **팔레트를 프로젝트로 통합** — 코드상 완전히 같은 것이었고, 사이드바 섹션 2개와
  "Add to…" 메뉴 2개만 만들고 있었습니다

---

## 6. Fork — 절반만 되던 기능

읽지 않고 **실제로 폰트를 내보내 결과물을 검사**했습니다(`--fork` CLI, 리포지토리에 남김).

**되고 있던 것:** UFO3 필수 파일 전부 정상, 곡선 형식 정확(OTF 3차 / TTF 2차),
가변폰트 마스터 18개가 실제로 서로 다름(축 좌표 정상 적용), fvar 인스턴스 18개 정확히 읽음

**고친 것:**
- **글리프 517 → 657 / 657.** 유니코드에 매핑된 것만 내보내고 있어서 대체자·합자·
  스몰캡이 통째로 빠졌습니다. 글리프 번호를 직접 순회하도록 변경
- **합성 글리프 528개 구조 보존.** `Á À Â Ä`가 독립 외곽선이 되던 문제.
  이제 `<component base="U"/>` 참조로 나가서 `A`를 고치면 따라옵니다
- **사전 손실 안내.** 내보내기 전에 무엇이 빠지는지 화면에 표시

범위를 좁힌 이유: **TrueType만** (CFF는 구조가 다름), **가변폰트 마스터는 제외**
(`gvar`가 마스터마다 컴포넌트 위치를 옮기는데 기본값만 읽으므로, 그대로 쓰면 악센트가
틀어집니다 — 틀린 구조보다 맞는 외곽선이 낫습니다)

- **커닝 47,017 페어 복원.** GPOS 페어 위치지정을 직접 파싱해 `kerning.plist`로
  내보냅니다. **fontTools가 뽑은 숫자와 정확히 일치**하고 건너뛴 서브테이블 0개입니다
  (`A → C: -66`처럼 값도 정상)

**여전히 빠지는 것:** OpenType 피처 18종(`features.fea`), CFF 합성 글리프,
가변폰트 마스터의 합성 구조

---

## 7. Fork를 별도 툴로 뺄 것인가 — **네, 그리고 방식도 바꾸십시오**

### 결합도는 이미 거의 없습니다

Fork 관련 코드 **1,853줄**이 앱 나머지와 닿는 지점:

| 방향 | 개수 |
|---|---|
| 앱 → Fork | **2곳** (`ContentView`의 라우팅 1줄, `SaewooFontApp`의 CLI 훅) |
| Fork → 앱 | `FontItem`(데이터 구조), `FontLibrary`·`SelectionModel`(ForkView가 선택 확인용) |

`UFOExporter`가 `FontItem`에서 실제로 필요한 건 **파일 경로 + 이름 몇 개 + 가변 축**
정도입니다. 나머지 필드는 구조체를 복사하느라 스쳐갈 뿐입니다.
**분리 비용은 거의 0입니다.**

### 그런데 더 중요한 문제가 있습니다

남은 작업(커닝·피처·CFF)을 Swift로 직접 구현하려던 참에 측정해봤습니다.
이 컴퓨터에 **fontTools 4.62.1이 이미 설치**돼 있습니다.

| | Swift 구현 | fontTools |
|---|---|---|
| GPOS 커닝 | **456줄 · 결과는 동일(47,017)** | **20줄 / 0.1초** |
| 합성 글리프 | TrueType만, 가변폰트 제외 | 전 형식, `addComponent` 기본 제공 |
| `features.fea` | 사실상 역컴파일러 필요 | `feaLib` |
| 가변폰트 마스터 | `gvar` 미적용이라 평탄화 | `varLib.instancer`가 정확히 적용 |
| UFO 규격 | 손으로 XML 조립 | `ufoLib2` |

커닝은 Swift로도 **정확히 같은 결과**를 냈습니다(456줄 들여서). 즉 "Swift로는 못 한다"가
아니라 **"같은 결과에 20배의 코드와 검증 비용이 든다"** 가 정확한 표현입니다.

그런데 남은 항목은 커닝보다 훨씬 어렵습니다. `features.fea`는 사실상 역컴파일러이고,
CFF 합성 글리프는 서브루틴 해석이 필요합니다. 그리고 `varLib.instancer`는 지금 포기한
"가변폰트 마스터의 합성 구조 보존"을 **정확하게** 해결해줍니다 — 이건 Swift로 직접
하려면 `gvar` 델타를 컴포넌트 오프셋에 적용해야 하는, 커닝보다 훨씬 까다로운 작업입니다.

### 권고

**Fork를 별도 툴로 분리하고, 폰트 포맷 처리는 fontTools에 위임하십시오.**

이유:
1. **결합도가 이미 0에 가까워** 분리 비용이 거의 없습니다
2. **사용자가 다릅니다.** 폰트를 정리하는 사람과 폰트를 그리는 사람은 다른 일을 합니다
3. **완성도 기준이 다릅니다.** 폰트매니저는 90%여도 쓸모 있지만, UFO 익스포터가 90%면
   **조용히 틀린 소스 파일**을 만듭니다 — 커닝이 없다는 걸 다시 그리기 시작한 뒤에 압니다
4. **의존성 판단이 갈립니다.** 일반 사용자용 맥 앱에 Python을 얹는 건 무겁지만,
   타입 디자이너용 도구는 **이미 fontTools를 깔아둔 사람들**이 씁니다
5. **릴리스가 분리됩니다.** 폰트 포맷 엔지니어링에 폰트매니저 릴리스가 묶이지 않습니다

**남길 것 / 옮길 것:**
- 폰트매니저에 남김: 없음. Tools에서 Fork 항목 제거, "선택한 폰트를 Fork로 보내기"
  정도만 (파일 경로만 넘기면 됨)
- 새 툴로: `UFOExporter` / `GlyfComponents` / `UFOFidelity` / `ForkView` / `ForkCLI`
- 새 툴에서 재작성 권장: 포맷 처리 전반을 fontTools 기반으로. Swift는 UI만

**분리하지 않기로 하신다면**, 현재 상태(글리프 전량 + TrueType 합성 구조 + 손실 안내)에서
멈추고 커닝·피처는 포기하는 편이 낫습니다. 어설프게 반쯤 구현하면 "있는 줄 알았는데
없더라"가 되어 오히려 위험합니다.

---

## 8. 남은 우선순위

1. **Fork 분리 결정** — 위 권고 참고. 결정 전까지 Fork에 추가 투자 보류
   - 어제 중단 지점: 커닝까지 완료. 다음은 `features.fea` / CFF composite /
     가변 마스터 composite — 셋 다 커닝보다 어렵고, fontTools에 이미 있습니다
2. **60 MB JSON 캐시를 바이너리 포맷으로** — 실행 1.2초와 피크 메모리를 한 번에 잡는 유일한 항목
3. **중복 제거 실사용** — 도구는 준비됐고 안전 검증도 끝났습니다. 처음엔 몇 그룹만
   체크해서 돌려보시길
4. `NSTableView` reentrant 경고 — 지금은 무해하나 향후 assert로 바뀐다고 예고된 메시지

---

## 검증 도구 (리포지토리에 남김)

```bash
swift build -c release
./.build/arm64-apple-macosx/release/SaewooFont --bench             # 성능 회귀 확인
./.build/arm64-apple-macosx/release/SaewooFont --scan-duplicates   # 중복 안전성 감사 (삭제 안 함)
./.build/arm64-apple-macosx/release/SaewooFont --fork <폰트> <폴더> # UFO 내보내고 규격 검사
```

`--bench`와 `--scan-duplicates`는 쓰기 잠금을 걸어 사용자 데이터를 건드리지 않습니다.
성능 수치는 반드시 `-c release`로 재세요. debug 빌드 수치는 의미 없습니다.
